# mhtran-dash

Multi-hazard transmission dashboard. Backend (FastAPI + PostGIS) + frontend (MapLibre + deck.gl) for the mhtran research outputs.

## Run

    docker compose up --build

API at http://localhost:8035, OpenAPI docs at http://localhost:8035/docs.